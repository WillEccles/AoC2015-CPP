#include "stdafx.h"
#include "Day1.h"
#include <iostream>
#include <fstream>
#include <string>

using namespace std;

// since I already did 1-3 in Java, this is going to be as minimal as possible, though not a one-liner unless possible

void Day1::run() {
	int f;
	string l;
	for (string p : getline(ifstream("Day 1 Input.txt"), l)) {

	}
}